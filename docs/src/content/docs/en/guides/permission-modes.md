---
title: Permission modes
description: The four permission modes — default, acceptEdits, plan, bypassPermissions — and when to use each.
---

This page is pending translation (ADT-51).

You can switch permission mode from the widget's settings panel. The default
is `acceptEdits`. `bypassPermissions` is exposed only inside the settings
panel — never from the chat composer — and should be used sparingly.
