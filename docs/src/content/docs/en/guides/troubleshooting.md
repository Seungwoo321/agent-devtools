---
title: Troubleshooting
description: Common errors — pairing token mismatch, ACP handshake failure, SDK rate-limit — and how to fix them.
---

This page is pending translation (ADT-54).

What to do when the widget is up but no responses come back, when the
`claude` CLI fails the stdio JSON-RPC handshake, or when the SDK provider
runs into your Pro subscription's 5-hour rate limit.
