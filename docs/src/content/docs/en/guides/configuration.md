---
title: Configuration reference
description: All Vite plugin options, provider environment variables, and widget settings in one place.
---

This page is pending translation (ADT-54).

A single reference for every option exposed by the `agentDevtools()` Vite
plugin, every provider-specific environment variable, and every value you
can tune from the widget's settings panel.
